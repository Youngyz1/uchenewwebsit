name: Test Slack Notification

on:
  push:
    branches:
      - cicd  # You can use any branch you push to for testing

jobs:
  slack-test:
    runs-on: ubuntu-latest
    steps:
      - name: Notify Slack
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          MESSAGE: "✅ Test message from GitHub Actions worked!"
